# SmartKegerator Python dependencies
# Install on Raspberry Pi OS Bookworm with:
#   pip install -r requirements.txt

# UI
PyQt6>=6.6.0

# Graphing (replaces Qwt)
pyqtgraph>=0.13.3

# Camera and image processing
opencv-python>=4.8.0

# Facial recognition (dlib ResNet model — best accuracy on Pi 4)
face-recognition>=1.3.0

# GPIO (modern Linux chardev API — replaces unmaintained wiringPi)
gpiod>=2.1.0

# DHT22 temperature/humidity sensor
adafruit-circuitpython-dht>=3.7.0

# Config file parsing
PyYAML>=6.0

# Numpy (pulled in by OpenCV and face-recognition, pinned for Pi compat)
numpy>=1.24.0

# Web interface
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9   # form data parsing
aiofiles>=23.0.0           # static file serving
httpx>=0.27.0              # async HTTP client for Untappd API proxy
itsdangerous>=2.1.0        # required by SessionMiddleware (optional dep of starlette)
PyJWT>=2.8.0               # JWT tokens for the mobile REST API
google-auth>=2.29.0        # FCM push notifications (OAuth2 for service account)
